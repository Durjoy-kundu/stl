#include <bits/stdc++.h>
using namespace std;
int main(){
//sorted unique and sorted elements
set<int> st;
st.insert(0);
st.insert(1);
st.insert(3);
st.insert(4);
st.insert(5);
st.insert(2); //{0,1,2,3,4,5}

auto it = st.find(0); // returns the position of the elemnt



return 0;
}